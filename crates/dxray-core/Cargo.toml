[package]
name = "dxray-core"
version.workspace = true
description = "Static analysis, launcher discovery, and Proton policy reading for Windows games on Linux."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords = ["games", "steam", "heroic", "proton", "pe"]
categories = ["development-tools"]

[dependencies]
dxray-pe = { path = "../dxray-pe", version = "0.2.0" }

[features]
# Test-only filesystem fixtures shared by crate consumers. This feature has no
# dependencies and is never enabled by the released binaries.
test-support = []

[lints]
workspace = true
